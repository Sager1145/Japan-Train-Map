# GitHub Pages 页面加载崩溃 — 架构与性能诊断

日期：2026-07-16 · 线上地址：`https://sager1145.github.io/Japan-Train-Map/`

## 一句话结论

每次打开页面，前端都在**主线程冷解算 119 趟列车、共 1,116 个路段**（Dijkstra +
构建最多 60 万节点的图，每个节点挂 4 个 `Set`）。本该充当缓存的
`matched-routes.json` 已经**过期，覆盖 0/119 趟**，IndexedDB 首访又是空的，于是
"预生成好、客户端不用算" 的快路径被完全绕过。结果是主线程长时间冻结 + 内存暴涨，
浏览器判定为崩溃（Chrome "Aw, Snap!" / "页面无响应"）。

## 为什么已有的修复没能解决

`deploy-pages.yml` 的注释把崩溃归因为 "无扩展名文件未压缩、12MB/3.3MB 裸传 +
一次性解析 JSON"，并用加 `.json` 扩展名让 Pages gzip 来 "修复"。这个修复**是对的、
也已经生效**——但它只减小了**下载体积**，完全没有触及**主线程计算量和内存占用**，
而后者才是真正的崩因。所以崩溃依旧。这是一次不完整的诊断。

---

## 根因 1（决定性）：加载时冷解算 1,116 个路段 — 严重

实测数据：

| 指标 | 数值 |
|---|---|
| 种子列车数（train-store.json） | 119 |
| 带内联路径几何的列车 | **0 / 119**（只存 from/to 站码，无坐标） |
| 需冷解算的路段总数 | **1,116**（平均 9.4/趟，最多单趟 62 段） |
| `matched-routes.json` 覆盖的列车 | **0 / 119**（且 train_id 命名空间对不上，纯过期） |
| Web Worker / 后台线程 | **无**（解算全在主线程） |

调用链：`DOMContentLoaded`(app.js:1600) → `replaceTrainStoreFromStoreProgressive`
(2589) → `runProgressiveAppend`(2451) → 每趟 `warmRouteCacheForTrain`(2501) →
`generateMatchedRouteFeaturesForTrain`(8514) → 每段 `solveRouteSectionOnDemand`
(9174) → 构建区域图 `getRegionalRouteGraph`(9082) + Dijkstra(9529)。

关键问题点：

- **让出（yield）只发生在"趟与趟之间"**：`runProgressiveAppend` 在循环顶部检查
  12ms 帧预算(2463/2485)，但一趟列车的整段解算是在检查之后一次跑完的。所以一趟
  62 段的列车会**连续阻塞主线程数秒**，中途无法让出 → "页面无响应"。
- **`matched-routes.json` 不是种子缓存**：代码在 8620-8623 是把新解算结果 *push 进*
  这个对象（当输出累加器用），并不会用它来跳过解算。它上次更新还是首个提交
  `045b5394`，之后列车数据换过多次（`f18f350`/`df5d272`/`fbbaa55`），从没同步重建。
- 首访者 IndexedDB 路径缓存为空，`runtimeRouteCache` 也是空的，所以 1,116 段全部
  从零解算。

## 根因 2：内存被同一份数据放大数倍 → OOM — 严重

加载后同时常驻内存的大结构（都源自那份 12MB 的 rail-sections）：

- `railSectionsGeoJson`：解析后的 21,933 个要素 / 404,646 个点（JS 里约 40-50MB）。
- **区域路由图** `regionalGraphCache`：加载期预算被抬到 **600,000 节点**
  (`REGIONAL_GRAPH_LOAD_NODE_BUDGET`, 9054)，每个节点带邻接表 + **4 个 `Set`**
  (line_names/operators/institution/railway_class, 8824-8829)。
- **完整全国图** `getRuntimeRouteGraph`(8900)：约 **377,000 节点**，任何跨区长途
  （新干线/寝台）触发边界回退(9207-9214)时就在主线程构建一次，≈377 万个 Set 对象。
- `_statsEdgeIndex`(4795)：约 40 万条目的 Map + 两个 40 万数组。
- `railSectionSpatialIndex`(8950)：要素按 0.1° 网格复制进它覆盖的每个格子。

同一份 12MB 数据实际被**再物化了三份**（stats 索引、路由图、空间索引），其中带 4
个 Set 的图节点是最大内存黑洞。峰值可轻松到几百 MB 甚至过 1GB → 触发渲染进程 OOM。

## 根因 3：全国路网作为单个客户端 GeoJSON 源 — 高

`jp-2025.json` 展开成 **9,442 条 segment（约 95,000 个顶点）+ 10,034 个站点**，作为
**单个** GeoJSON 源交给 MapLibre(railmap.js:572-580)。即使图层 `visibility:"none"`
(615/637)，MapLibre 的 worker 仍会在地图初始化时把整份全国网 parse + geojson-vt
建索引。这是本文件域内最大的一次性内存/CPU 尖峰，叠加在上面两条之上。

## 次要 / 性能问题

- **`getRailContentHash`(2196-2231)**：在 `warmRouteCacheFromIndexedDb` 里，对全部
  ~40 万个点做一次同步遍历混哈希，落在关键启动路径上（几十 ms 的阻塞）。
- **hover 热路径**：`_pushHoverRegions` 每个 hover 帧无条件 `JSON.stringify` 一个诊断
  对象并写入 dataset(railmap.js:2165)——即便调试面板关着；淡入淡出期间 `_applyDimPaint`
  每帧重建表达式并发 13 次 `setPaintProperty`(2522/2582-2596)。
- 自动聚焦（focus zoom）一次选择约 4s（app.js:1538-1543 注释自述），已默认关闭。

## 已排除的疑点（不是问题，别在这上面花时间）

- **preload `crossorigin`**：`index.html:40-41` 的 preload 与 `fetch()` 对同源资源的
  CORS/凭据模式**是匹配的**，preload 会被复用，**没有双重下载**。
- **SSE `./api/events` 404**：按 EventSource 规范，非 200 响应会**一次性失败**
  (readyState=CLOSED)，**不会重连风暴**。`onerror` 是空操作。
- **autosave `PUT ./api/train-store.json`**：启动阶段根本不调用
  (`persistEachStep/finalPersist:false`)，失败也不重试风暴。
- **`.json` gzip 修复**：已部署且正确，只是没对准崩因。

---

## 修复建议（按性价比排序）

1. **重建 `matched-routes.json` 覆盖全部 119 趟**（或把解算好的路径几何直接内联进
   `train-store.json` 的 `route_sections`）。这恢复了本就设计好的"预生成"快路径，
   客户端加载时直接读几何、**完全不再冷解算**。改动最小、收益最大。落地方式：写一个
   一次性脚本，用现有解算器离线跑一遍 119 趟、导出几何，纳入部署流水线。
2. **加载时绝不在主线程冷解算**：没有缓存的列车，把解算推迟到 `requestIdleCallback`
   或"被选中/进入视口时"再算；理想情况把 Dijkstra 搬进 **Web Worker**，主线程只收结果。
3. **逐路段让出**：把 12ms 帧预算检查下沉到 `routeSections.forEach` 内部，让单趟重列车
   也能中途让出，消除多秒级冻结。
4. **全部路线预生成后，客户端干脆不加载 12MB 的 rail-sections**：解算器数据只在需要
   现算时才拉取，可省下最大的一块内存。
5. **给图内存封顶**：调低 60 万节点预算、节点不再挂 4 个 Set、更激进地淘汰缓存图。
6. **全国路网懒加载或改矢量瓦片**：仅在用户首次开启该图层时再加载，而非启动即全量解析。

其中第 1 条基本可单独消除崩溃；第 2/3 条保证即使偶发现算也不再冻结；第 4/5 条把内存
峰值压到安全区。

---

## 已实施的修复（2026-07-16）— 逐路段流式解算

按"通用型"要求（任意规模的合法导入数据都要能正确生成路径），没有走"预烤 seed"的
第 1 条，而是直接改造解算器本身（对应上面的第 2/3/5 条），改动集中在 `app/public/app.js`：

- **逐路段让出（消除冻结）**：把原本"一趟列车一次性同步解算全部路段"的热点
  （`generateMatchedRouteFeaturesForTrain`）拆成 `prepareTrainRouteSolve` / `solveTrainRouteSection`
  / `commitTrainRouteSolve` 三个共享子过程，并新增异步流式版 `warmRouteCacheForTrainStreaming`：
  **一次只解算一个路段，跑满约 12ms 就让出一帧**给浏览器（绘制 + 响应输入），再继续。
  这样即使单趟 62 个路段、或一次导入上千趟，主线程也永不长时间冻结（不再"页面无响应"），
  帧间还能触发 GC 回收临时图内存（缓解 OOM）。
- **共享帧预算 + 周期性内存封顶**：`runProgressiveAppend` 用一个贯穿"路段与列车"的
  统一预算时钟驱动让出，并在每次让出时 `trimRegionalGraphCache` 收敛区域图缓存，导入
  规模再大峰值内存也有界；加载结束仍回落到稳态预算（`finalizeProgressiveLoad`，300k）。
- **渲染路径不再冷解算（关键正确性）**：加载期间（`importInProgress`）渲染只画"已解好
  缓存命中"的列车；未轮到解算的列车这一帧跳过、下一帧补画——绝不在渲染线程上同步解算，
  否则会把刚消除的冻结又引回来。相应地，加载期渲染对未解算列车不再打出吓人的
  "No N02 railway path…"告警。
- **通用**：导入与首屏加载走的是同一条 `runProgressiveAppend` 路径，所以此修复对
  **任意规模的合法 train-store 数据**（完整 store / 列车数组 / 单列车对象）一视同仁地
  生效——每加载一段就显示一段，边解边画。同步解算器逻辑本身逐字保留（校验通过），
  路径结果不变。

**验证**：`node --check` 通过；解算器拆分的逐条不变量校验全部通过（缓存/负缓存/去重/
matched-routes 更新/状态文案均与原实现一致）；一次对照备份的对抗式代码复查结论为
"SAFE TO SHIP"（同步路径行为等价、导入期延后对所有调用方都是 `[]`-安全、无未 await 的
Promise、内存预算生命周期完整）；部署工作流对 `app.js` 的取数路径改写锚点未受影响。

**残留（可选后续）**：跨区长途首次触发的"完整全国图（约 37.7 万节点）"构建仍是一次性
不可分块的阻塞（数百 ms 级，构建后记忆化不再重复）。如需进一步平滑，可把该图构建也
分块让出，或改由 Web Worker 承担——属独立后续项，不影响本次崩溃修复。
