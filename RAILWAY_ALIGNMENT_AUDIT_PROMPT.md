# 上下行分线 / 支线走向 审计 prompt

对某条铁路（或某一段）审查「两个方向是否走不同轨道」「支线走向是否画对」。
每条检查都给出**判据、方法、和已知会骗过你的反例**。规则本体见
[`RAILWAY_DATA_TOPOLOGY_AND_APPLE_MAPS_DISPLAY_RULES.md`](./RAILWAY_DATA_TOPOLOGY_AND_APPLE_MAPS_DISPLAY_RULES.md) §7.7。

## 使用方式

把下面「审计任务」整段作为 prompt 交给一个 agent，把 `<线路>` 换成目标。
一次只查一条线路或一个区间；结论必须带**可复算的数字**，不接受「看起来对」。

---

## 审计任务

你在审查 `<线路>` 的上下行走向与支线绘制。**只报可复算的结论**：每条判定
必须给出测得的数值、测法、和数据出处。没量过的写「未测量」，不要推断。

### 阶段 0 · 先建立事实，不要先看地图

地图是**最后**的验收，不是判据来源。截图会让你先形成结论再去找证据。

1. 从包里取该线路及其兄弟笔画：id、站数、里程、`alignmentRole`/`alignmentOf`/
   `alignmentDirection`/`alignmentSource`/`alignmentSplitSource`。
2. 从 `data/raw/railway/jp/rebuild-inventory/stations/n02-station-connections.csv`
   取该线路全部邻接边的 `distance_km`、`network_status`、`connection_type_tags_json`。
3. 记下**本线站序方向**：`stations[0] → stations[-1]`。这一步不能跳过，见 C4。

### 阶段 A · 该不该有第二条走向

**A1 逐区间量分离幅度，点到线段。**
对每个区间，量两条候选笔画（或该线路两个站台之间）的**最宽间距**，
距离必须是**点到线段**，不是点到最近顶点。

- 判据：≥100 m 才是候选。
- 反例：N02 隧道顶点间距约 270 m，按顶点量会虚增约一半 ——
  倶利伽羅 按顶点 180 m、按线段 **40 m**（同一条隧道的两个洞）。
- 已知刻度：笹子/新笹子 25 m · 倶利伽羅 40 m · 新子不知 124 m · 清水/新清水 840 m。

**A2 剔除四类假阳性**，逐条给出否决理由：

| 类型 | 例子 | 识别方式 |
| --- | --- | --- |
| 单向环线，一圈经过两次 | ディズニーリゾートライン 1,340 m · ユーカリが丘線 883 m | 该线是单向运行环 |
| 同键下的平行实体线 | 山手線 代々木–新宿 616 m · 東北線-2 御徒町–秋葉原 820 m | 電車線/列車線、複々線 |
| 貨物線 / 連絡線 | 西日本 東海道線-2 大阪–福島 **3,427 m**（梅田貨物線）· 白新線 東新潟–大形 2,053 m | 官方営業キロ 与跨度不符 |
| 并行的另一条铁路 | 千歳線 白石–平和 1,255 m（函館線 并行＋札幌貨物ターミナル 引入线） | 官方営業キロ 2.2 km 对跨度 4.71 km |

**A3 官方営業キロ 与跨度对不上就是警报。** 千歳線 那条就是靠 2.2 km 对 4.71 km 露馅的。

### 阶段 B · 两条 bore 分别是哪一条

**B1 找具名构造物。** 从 OSM 取 `tunnel:name`，或从来源取隧道/桥梁名。
「新」字前缀通常是后建的那条（新清水 / 新松川 / 新立石 / 新田原坂）。

**B2 按名字排除无关线路的构造物。** 不做这一步，判据**无论真相如何都会给出答案**：

- 金谷川 旁 11.8 km ×2 = **東北新幹線** 福島トンネル → 会把 24 km 新干线隧道算到在来線笔画上
- 田原坂 旁 2,944 m = **九州新幹線** 新田原坂トンネル（在来線那条是 942 m）
- 陣場 旁 3,177 m ×2 = 矢立トンネル，**複線断面**且在跨度之外（两笔离它都 4.6 km，正好反证选对了洞）

**B3 从构造物自身的顶点量到两条笔画，取近者。**
方向不能反：笔画→构造物会被笔画长度稀释。

- 对：日豊線 从 新立石トンネル 顶点量 → 主線 28 m、配對 343 m
- 错：同一案例反向量 → 543 m 对 577 m，毫无区分度

**B4 用第二个独立判据交叉验证。** 至少两个之一：

- **审计里程**：主线应与审计相符（日豊線 主線 5.18 km 对审计 4.968；配對 6.38 km）
- **螺旋摆幅**：见 B5
- **地理位置**（有来源支持时）：如「下り是靠海那条」

**B5 螺旋按最大连续转角判，不按净转角。**
沿线累计转角的**峰谷差**；净值会互相抵消。

- 上越線 上り 净 **−120°**、摆幅 **405°**；下り 摆幅 143°
- 门槛：上り >300°、下り <250°
- 松川ループ 上り 492°、下り 201°

### 阶段 C · 方向标注

**C1 先确认「分离」与「方向」各自的来源。** 两个独立主张。分离有据而方向无据时，
方向必须写 `unassigned`，并把来源放进 `alignmentSplitSource`。
上伊集院–広木 就是此类：分离由 ja.wikipedia 明述，方向只有一篇自相矛盾的博客。

**C2 来源必须有可引用的日文原句。** 没有原句的一律记「unsourced」。

**C3 博客级来源只能降级使用**，且必须写明与哪条官方表述冲突。

**C4 换算运营方词面到本仓约定 —— 这一步最容易错。**
本仓 `down` = **站序前进方向**。站序是运营方上り的线路必须反转：

```text
日豊線     小倉→大分     = 运营方 下り   → 不反转
鹿児島線   八代→門司港   = 运营方 上り   → 反转
奥羽線     青森→福島     = 运营方 上り   → 反转
北陸線     敦賀→米原     = 运营方 上り   → 反转
中央線     みどり湖→神田 = 运营方 上り   → 反转
```

照抄词面会把**每次乘车判到错的股道上**。每条证据行写明该线换算。

### 阶段 D · 几何是否贴合真实轨道

**D1 逐区间量到「任意真实轨道」的距离**（不是只量到某一条 bore）。
中位数应 ≤20 m，最大 ≤50 m。

- 反例：量到单条隧道 way 会在洞口之后误报 656 m，其实离**任意轨道** 7 m。

**D2 N02 的第二条 bore 中心线常常很粗。** 上越線 湯檜曽ループ 曾离真实轨道 **222 m**。
需要 OSM 几何时：

- 按名字**封掉另一条 bore** 再路由。最短路**结构上就是新线那条** ——
  直接跑会把 松川ループ 返回成下り线，并把 湯檜曽 螺旋剪成 120° 弯。
- 两个端点取自**同一连通分量**。越後中里 的最近顶点在侧线分量上，
  由此产生的「无路径」是**假阴性**，曾让整段被误记为「OSM 没有上り股道」。
- 长度要与审计/N02 对得上（3.670 对 3.681、10.363 对 10.341）——这是「没跑到另一条 bore」的校验。
- **只替换已判定分离的区间**；引入段保留 N02 以与主线逐点重合，换源即产生假平行轨。

**D3 首尾必须落在主线笔画上（<1 m）。** 分离段两端都要接住：
远端在会合站，近端在**两方向仍共用的最后一站**。
compact-v1 没有「首站之前的轨道」这个位置（`rail-network.js:734` 会把端点钉到站点行）。

### 阶段 E · 支线 / 服务边 / 轨道组

**E1 不要用「离车站多近」判定「是否同一条轨道」——两个分布重叠。**

- 佐世保線 真服务边离 早岐 **334 m**，确是同轨
- 旁通轨道可以离站 **75 m** 仍是另一条轨

按距离排序在任何方向都不可能。要判同轨须问**是否走同一条 N02 区段**，
或直接问轨道图：切掉第一条路径后，**有没有第二条边不相交的路径**。

**E2 「有没有第二条路径」要问对，问错会得到假阴性。**
函館線 七飯–大沼 的 藤城線 我第一次判成「N02 只有一条路径」，**是错的**。错法是
`path_between(A,B,exclude=第一条路径的全部区段)` —— 这会把两条走向**共用的接近段**
一起封掉，于是任何第二条路都不存在。

正确问法两种：

- 只排除**被跳过那一站自己的区段**，再路由。函館線 由此得到 13.309 km 走 section 19748，
  离 新函館北斗 最近 **2,898 m**。
- 或者直接枚举该区域属于本线的 N02 区段，看**哪些是第一条路径没用到的**。
  函館線 盒内 16 段，第一条用 9 段，剩 7 段共 32 km —— 其中 **section 19748（10,916 m）
  正是藤城線**。

**「审计 16.263 km 是异常值」这半句也错了，2026-08-17 复审推翻。** 那条审计边是
大沼↔新函館北斗（n02-station-connections.csv 3714/3715 行），七飯↔大沼 直连边另是
13.0875 km（3716/3717 行）——两条都**骑在 藤城線 上**，复算分别为 16.504 km 与
13.309 km，审计值系统性偏短 1.5–1.7%，正常。当时的「异常」推理拿另两条边推出
9.2 km 当应然值，可这条边量的根本不是经 新函館北斗 的那条路。教训：断言一条边异常之前，
先查它**连的是哪两站、骑的是哪条轨**。藤城線 现已绘出（jp-2025 函館線 的 p1 笔画）。

**E2b 別線 有两种形状，只有一种能从站台看出来。**

| 形状 | 例子 | 检测方式 |
| --- | --- | --- |
| 站台分离 | 上越線 湯檜曽/土合 各两个站台 | N02 第二个站台要素 |
| **跳站** | 函館線 藤城線 跳过 新函館北斗・仁山 | **站台测不出**——两端各一个站台，该有两个的那些站根本不在线上 |

跳站型还有一个陷阱：**经过被跳站的那条路更短**（13.285 对 13.309），所以最短路
永远给不出它，而跳站判据（沿最短路切开再看经过哪些站）会因此把它误判成服务边——
它报「大沼–七飯 runs past 新函館北斗」，而这条边自己的轨道离那站 **2.9 km**。

**E3 轨道组拆分点的车站必须两侧都有。** 否则跨越边界的区间没人画 ——
札幌 因此丢了 桑園–札幌 1.314 km 且 函館 侧没有 札幌。
检查方式：对每条有多个轨道组的线路，看相邻两段的首尾站是否同一站。

**E4 每条新增连接都要在审计里找到对应边**，并核对 `network_status=active`。
画出来的里程比审计长 15–25% 属正常（站台中点锚定＋真实曲率）；差一倍就是走错路。

### 阶段 F · 可复现性与回归

**F1 `PYTHONHASHSEED=1,2,3` 各建一次，产物必须逐字节相同。**
任何为顺序敏感用途迭代的集合都是嫌疑：`longest_path()` 迭代 `near[node]` 集合，
曾让 東海道線 与兄弟笔画每次重建互换 15 km。等距叶子要按站号打破平局。

**F2 与上一次构建逐线对比**，列出站数/里程变化的每一条并逐条解释。
只该动你打算动的那几条。

**F3 全量门禁**：`npm test` · `npm run lint` · `npm run build` ·
五国 `validate-railway-topology.mjs` · `validate-station-render-anchoring.mjs`。
特征值（render hash、站数、segment 数、徽章计数）变动要**写明理由**，
不得为了让测试通过而改断言。

**F4 最后才看地图。** 截图用于确认，不用于推断。

### 输出格式

```text
线路：<线路>            站序：<首站> → <末站>（= 运营方 上り／下り）
1 该不该有第二条走向   候选区间 / 分离幅度（点到线段）/ 否决的假阳性及理由
2 哪条是哪条           构造物名 · 从构造物顶点量得的两个距离 · 第二判据（审计里程／摆幅）
3 方向标注             分离来源 · 方向来源 · 换算 · 或 unassigned 及原因
4 几何贴合             逐区间 中位/最大 离任意真实轨道 · 首尾 gap
5 支线与轨道组         第二条路径是否存在（切一次再问）· 拆分点站是否两侧都有 · 新增边对应的审计行
6 复现与回归           三个 hash seed 是否逐字节一致 · 逐线 diff · 门禁结果
7 未解决               每条写明缺什么（缺来源／缺 N02 轨道／缺审计记录）
```

## 已知不是缺陷的（不要重复报）

- **中央本線 甲斐大和–笹子**：笹子/新笹子 相距 25 m，普通复线。
- **常磐線 天王台–我孫子**：線路別複々線 快速/緩行。
- **東北線 矢幅–岩手飯岡**：盛岡貨物ターミナル 站场。
- **あいの風とやま鉄道 倶利伽羅–石動**：同一条隧道两个洞，实际 40 m。
- **千歳線 白石–平和**：函館線 并行＋貨物 引入线。
- **日豊線 西屋敷–中山香**：分离到 立石 结束，跨度应取 西屋敷–立石。

## 已知缺陷但需要资料修订，不是几何问题

- **仁山駅** 完全不在 2026-08-13 审计资料里，因此画不出来。
- **中央線 勝沼ぶどう郷–甲斐大和** 分离有据，但 N02 在那两站只存一个站台，检测器不会报。
- **札幌** 两个轨道组锚点相距 208 m，是 N02 站内区段的真实缺口，未桥接。
