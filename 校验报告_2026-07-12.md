# train-store.json 核查报告 — 2026-07-12

对象：`app/data/train-store.json`（117 → 113 趟），依据 `jsonspec.md`（schema 1.3）。
修改前备份：`app/data/train-store.backup-20260712.json`。

## 核查项与结果

| 核查项 | 结果 |
| --- | --- |
| 顶层结构 / 字段白名单（train、stop、section）§2–§7 | ✅ 无多余键，id 全部唯一且合法 |
| route_policy（mode / alternatives / fallback / codes）§5 | ✅ 全部符合 |
| stops ≥2、首末站类型、首末站到发时刻规则 §7/§15 | ✅ 全部符合 |
| 时间格式 `HH:MM` §10 | ⚠️ 5 处「约…」非标准时间 → 已修 |
| 「一条记录 = 一趟列车」 | ⚠️ 8 条含多列车/多备选 → 已修 |
| n02_station_code 是否存在、与站名是否一致 | ✅ 有码的站全部正确 |
| route_sections line_names 是否为真实 N02 路线名 | ✅ 全部存在于 N02（553 条线名中） |
| allowed_institution_type_codes 与站点事业者种别一致性 | ✅ 无冲突 |
| 车号奇偶 vs 运行方向（上り偶数 / 下り奇数） | ✅ 全部有号列车方向一致 |
| 站点时刻单调递增、origin/destination 与首末站一致 | ✅ 全部符合 |
| 无码站名能否在 N02 解析 | ⚠️ 仅羽後交通巴士 8 个站名无法解析（非铁路）→ 条目已删 |

## 已修复（10 类，均经复检通过）

1. **7/3 新大阪→三島**「ひかり/こだま」→ **こだま号**（3h04m 与こだま各站停车吻合，经确认）。
2. **7/25 名古屋→三島**「ひかり/こだま」→ **ひかり号**（1h18m，こだま需约 1h40）。
3. **7/26 三島→東京**「ひかり/こだま」→ **こだま号**（54 分钟为こだま区间标准耗时）。
4. **7/13 青森→新青森**「奥羽本線 / つがる」→ **特急つがる**（经确认）。
5. **7/26 東京→新宿**「中央線 快速/普通」→ **中央線 快速**（14 分钟且東京站始发，各停不入東京）。
6. **7/26 河口湖→大月**「富士急行線 普通/接続」→ **富士急行線 普通**。
7. **7/26 大月→八王子**「中央本線 快速/普通」→ **中央本線 快速**（单条记录直达八王子，只能是直通快速；普通高尾行需换乘）。
8. **7/18 こまち+はやぶさ38号** → 按 §6.1b/§8.6.3 规范化：顶层 `こまち38号・はやぶさ38号`；route_sections 拆为 田沢湖→盛岡（`number: こまち38号`）与 盛岡→大宮（`number: はやぶさ38号`），并补 盛岡 停站保持 stops/sections 对齐。
9. **时间修正**：`约11:43`→`11:43`（7/13 青森）、`约16:00`→`16:00`（7/13 新青森）、`约18:11/18:14`→`18:14`（7/13 北斗19 新函館北斗発）、`约21:50`→`21:50`（7/18 八王子）、`约06:05`→`06:05`（7/23 山川）。
10. **删除 4 条羽後交通巴士**（乳頭線、田沢湖一周線×3）：非铁路，N02 无数据，无法渲染（经确认删除）。

保留未动（判定为合规）：`京急・浅草線`（直通＝同一列车贯通运行）、`中央・総武線`、`京浜東北・根岸線`（均为单一运转系统的正式名称）、`宗谷`/`ノサップ`/`はこだてライナー` 等无号但唯一可辨识的列车。

## 遗留问题（未修，需时刻表数据或确认）

1. **§8.6.1 全区间要求未满足**：约 40 趟特急/新幹線只录了乘坐区间（多为 2 站条目），如 はるか、とき308（实际运行至東京）、はくたか555/565、つるぎ17、しらさぎ2、ソニック44（实际运行至博多）、リレーかもめ22（至博多）、あしずり10（推测中村始发）、こまち24（实际运行 秋田→東京）等。补全需逐趟查 2026-03 改正时刻表（官方仅提供 PDF），可另行处理。
2. **车号未确认**：本次改为单一列车的 4 条（こだま号×2、ひかり号、特急つがる）只定了车种，具体号数待时刻表核实；另 `こだま918号` 的号数在可查资料中无法复核（方向奇偶正确）。
3. **约 45 条记录缺 `n02_station_code`**（运行时按站名解析，站名均可解析，功能正常）且缺 `allowed_institution_type_codes`（默认全量+soft，合规但建议收紧：新幹線→`["1"]`、JR 在来→`["2"]` 等）。
